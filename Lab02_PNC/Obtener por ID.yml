info:
  name: Obtener por ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/pirates/{{ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
